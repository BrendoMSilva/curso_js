let nomes = ['Pedro', 'Maria', 'Brendo'];

for(let i = 0; i <= nomes.length; i++) {
    console.log(nomes[i]);
}

let marcas = ['Nike', 'Adidas', 'Puma'];

for(let seguencia = 0; seguencia <= marcas.length; seguencia++) {
    console.log(marcas[seguencia]);
}

let numeros = [1, 4, 34, 66, 50];

for(let num = 0; num <= numeros.length; num++) {
    console.log(numeros[num]);
}