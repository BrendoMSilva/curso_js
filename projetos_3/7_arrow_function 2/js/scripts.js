const multiplicarPorDois = (x) => {
    return x * 2;
}

console.log(multiplicarPorDois(2));

const multiplicarPorTres = x => x * 3; // aconselhavel  usar apenas quando não tiver muitos parametros

console.log(multiplicarPorTres(3));